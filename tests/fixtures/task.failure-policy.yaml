apiVersion: sentinelflow.io/v1alpha1
kind: TaskSpec
metadata:
  name: example-partial-failure
spec:
  authorizationScope: fixture:local-only
  targets:
    - name: fixture-one
      input:
        message: partial failure fixture
  steps:
    - name: fails
      toolRef: example-failure
      capability: fail.fixture
      failurePolicy: continue
    - name: independent
      toolRef: example-echo
      capability: echo
      failurePolicy: continue
    - name: dependent
      toolRef: example-echo
      capability: echo
      dependsOn: [fails]
      failurePolicy: skipDependents
  policy:
    allowedTargets: [fixture-one]
    maxConcurrency: 2
    rateLimitPerMinute: 60
extensions: {}

