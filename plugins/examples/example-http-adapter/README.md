# Example HTTP Adapter

Safe loopback fixture. Start a local test service on `127.0.0.1:18080` before
running it. Non-loopback plaintext HTTP, cross-origin pagination, and cross-origin
polling are denied by the Adapter.

