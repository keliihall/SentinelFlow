# Example Restricted High Risk

Safe restricted fixture for P5.5 approval E2E tests.

This plugin performs only local bounded echo behavior. It is marked `high` risk
and `requiresApproval: true` solely to validate approval, policy, and audit
flows. It does not scan, probe, exploit, brute force, bypass authentication, or
contact external targets.

