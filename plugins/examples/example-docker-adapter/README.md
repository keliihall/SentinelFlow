# Example Docker Adapter

Safe contract fixture for a locally built image named
`sentinelflow/example-fixture:local`. Networking is disabled and CPU, memory,
timeout, and output limits are mandatory. The repository does not ship or pull an
image, so validation does not contact a registry.

