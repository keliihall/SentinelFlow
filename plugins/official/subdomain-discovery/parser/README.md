Uses SentinelFlow's trusted built-in `example-file-import-v1` parser. The plugin
runner emits a strict `source` and `records` structure, then SentinelFlow validates
the output Schema and the Normalizer produces a `ToolOutput`.

No parser code from this plugin directory is loaded in process.

