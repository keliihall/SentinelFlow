apiVersion: sentinelflow.io/v1alpha1
kind: ToolManifest
metadata:
  name: openvas-import-plus
  labels:
    sentinelflow.io/official: "true"
    sentinelflow.io/asset-category: vulnerability-import
spec:
  displayName: OpenVAS Import Plus
  version: 0.1.0
  capabilities:
    - name: data.import.openvas.report
      description: Imports bounded OpenVAS or Greenbone XML/CSV report fixtures into structured vulnerability findings without scanning targets
      risk: low
      requiresApproval: false
    - name: risk.vuln_import.openvas
      description: Normalizes OpenVAS vulnerabilities with host, port, NVT OID, CVE, CVSS, QoD, threat, severity, and remediation metadata
      risk: low
      requiresApproval: false
  runtime:
    adapter: command
    mode: process
    entrypoint: runner/runner.py
    args: []
    environmentAllowlist:
      - PATH
    timeoutSeconds: 300
    outputLimitBytes: 4194304
  parser:
    mode: builtin
    name: openvas-import-plus-v1
  inputSchema: schemas/input.schema.json
  outputSchema: schemas/output.schema.json
extensions:
  sentinelflow.io/category: data.import
  sentinelflow.io/riskLevel: low
  sentinelflow.io/safetyProfile: bounded-offline-openvas-report-import
  sentinelflow.io/networkBoundary: no-network-file-under-plugin-root-only
  sentinelflow.io/capabilities:
    - name: report_import
      riskLevel: low
      impact: read_only
      requiresApproval: false
  sentinelflow.io/controls:
    requiresAuthorization: true
    requiresApproval: false
    supportsRateLimit: false
    supportsConcurrencyLimit: false
    defaultMode: fixture
    maxConcurrency: 1
    secretTransport: none
  sentinelflow.io/audit:
    recordInput: true
    recordOutputSummary: true
    recordRawOutput: false
    evidenceRequired: true
