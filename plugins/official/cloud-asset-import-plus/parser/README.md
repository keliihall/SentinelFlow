# Built-in parser

The manifest selects `cloud-asset-import-plus-v1`, a trusted parser compiled
into `sentinelflow-runtime`. Schema-validated `cloud_asset_result` records are
normalized into `asset.cloud` findings with provider, scope, region, exposure,
address, DNS, tag, and security-rule evidence.
