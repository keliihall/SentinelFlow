# Built-in parser

The manifest selects `cmdb-sync-plus-v1`, a trusted parser compiled into
`sentinelflow-runtime`. Imported `cmdb_asset_result` records become
`asset.cmdb` findings. Reconciliation operations remain schema-validated values
for an approved, audited CMDB gateway to apply.
