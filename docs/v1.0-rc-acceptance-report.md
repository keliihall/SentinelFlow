# SentinelFlow v1.0-rc Acceptance Report

## Status

Candidate status: ready for local pilot after release gates pass.

## Requirement Evidence

| Requirement | Evidence |
| --- | --- |
| CLI/API/Web plan consistency | `tests/e2e/p5_5_consistency.sh`; API contract test `api_plan_matches_orchestrator_plan`; CLI planner is exercised in `cli_contract.rs`; Web calls `/api/tasks/plan`. |
| CLI/API/Web Policy Explain consistency | `tests/e2e/p5_5_consistency.sh`; API policy endpoint calls the shared policy evaluator; API contract test compares endpoint output with server-side `explain_policy`; CLI policy tests cover the same fixtures. |
| CLI/API/Web run, finding, report, and audit consistency | `tests/e2e/p5_5_consistency.sh`; `docs/release/p5_5_consistency_report.md`. |
| Complete Web/API loop | Full-flow e2e `tests/e2e/p5_5_full_flow/run.sh`; API contract test `api_supports_plugin_to_report_flow`; e2e script `tests/e2e/p5_5_smoke.sh`; Console workflow in `docs/v1rc-trial-guide.md`. |
| Success, approval, failure, and cancellation flows | Full-flow report `docs/release/p5_5_e2e_report.md`. |
| Auth, RBAC, audit | Security e2e `tests/e2e/p5_5_security/run.sh`; API test `protected_routes_require_authentication_and_role`; API flow test checks audit actions. |
| Security hardening and bypass resistance | Security hardening report `docs/release/p5_5_security_hardening_report.md`. |
| Reliability and abnormal recovery | Reliability e2e `tests/e2e/p5_5_reliability/run.sh`; report `docs/release/p5_5_reliability_report.md`. |
| Log reconnect | SSE cursor endpoint, API test `task_log_sse_reconnects_from_cursor`, Web Console reconnect test `web_console_reconnects_logs_with_cursor`, and reliability reconnect scenario. |
| Security failures | API test `api_rejects_rc_blocker_failure_modes`; CLI contract tests for authorization, high-risk, parser, missing runner, cancellation, and task failure. |
| Database migration and upgrade | Store migration tests; deployment e2e `tests/e2e/p5_5_deployment/run.sh`; report `docs/release/p5_5_deployment_report.md`. |
| Performance and capacity baseline | `tests/performance/run.sh`; report `docs/release/p5_5_performance_baseline.md`; raw metrics `docs/release/p5_5_performance_baseline_metrics.json`. |
| Single-machine deployment | `Dockerfile`, `compose.yaml`, deployment docs, and deployment e2e. |
| New-user trial documentation | `docs/v1rc-trial-guide.md`. |
| Release notes and changelog | `docs/release-v1.0-rc.md`, `CHANGELOG.md`. |

## Latest Local Verification

- `cargo fmt --all -- --check`: passed
- `cargo build --workspace`: passed
- `cargo clippy --workspace --all-targets -- -D warnings`: passed
- `cargo test --workspace`: passed
- `tests/e2e/p5_5_smoke.sh`: passed
- `tests/e2e/p5_5_consistency.sh`: passed
- `tests/e2e/p5_5_full_flow/run.sh`: passed
- `tests/e2e/p5_5_security/run.sh`: passed
- `tests/e2e/p5_5_reliability/run.sh`: passed
- `tests/e2e/p5_5_deployment/run.sh`: passed
- `tests/performance/run.sh`: passed
- `docker compose config`: passed
- `docker compose build sentinelflow-api`: passed
- `docker compose up -d`: passed
- `curl -fsS http://127.0.0.1:8080/health`: passed
- `curl -fsS http://127.0.0.1:8080/console`: passed
- `docker compose down`: passed

## Commands To Record For Release

```sh
cargo fmt --all -- --check
cargo build --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
tests/e2e/p5_5_smoke.sh
tests/e2e/p5_5_consistency.sh
tests/e2e/p5_5_full_flow/run.sh
tests/e2e/p5_5_security/run.sh
tests/e2e/p5_5_reliability/run.sh
tests/e2e/p5_5_deployment/run.sh
tests/performance/run.sh
```

## Residual Risks

- Development authentication is intentionally simple and must be replaced for any
  production pilot.
- Compose deployment is single-node and local-volume based.
- PostgreSQL is documented as reserved/not active for v1.0-rc; SQLite is the
  supported deployment backend.
- Performance baseline is scoped to single-node local pilots and synthetic
  example workloads; it is not evidence for distributed production scale.
- Docker image build time depends on compiling the Rust workspace inside the image.
